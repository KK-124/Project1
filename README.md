# Project1

This repository contains a simple command-line Tic Tac Toe game.

## Running the game

Run the following command to start playing:

```bash
python tic_tac_toe.py
```

You will play as `X` against the computer (`O`). Enter your moves in the format `row,column` using indices 0-2.
